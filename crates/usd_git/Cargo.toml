[package]
name = "usd_git"
version = "0.0.1"
edition = "2024"
license = "MIT"
description = "Read-only Git history and revision materialization for USDHub."
repository = "https://github.com/bresilla/bevy_openusd"

[dependencies]
gix = { version = "0.86", features = ["revision", "status", "tree-editor"] }

[dev-dependencies]
tempfile = "3"
