[package]
name = "usd_project"
version = "0.0.1"
edition = "2024"
license = "MIT"
description = "Pure Project domain identities and root semantics for USDHub."
repository = "https://github.com/bresilla/bevy_openusd"

[dependencies]
serde = { workspace = true }
thiserror = { workspace = true }
uuid = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
